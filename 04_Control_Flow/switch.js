const month=3;
switch (month) {
    case 1:
        console.log("January");
        break;
    case 2:
        console.log("Febaury");
        break;
    case 3:
        console.log("March");
        break; // this break is used to break to code that value matches the condition that we not used break that there will run all after condition matches execpt default code  

    default:
        break;
}